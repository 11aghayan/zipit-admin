import AddButton from "@/components/AddButton";

const Categories = () => {
  
  return (
    <main>
      <AddButton 
        label="Add Category"
        variant="category"
      />
    </main>
  );
};

export default Categories;