import ChangePasswordForm from "./components/ChangePasswordForm";

const Settings = () => {
  return (
    <main className="fixed p-2 inset-0 top-10 flex items-center justify-center">
      <ChangePasswordForm />
    </main>
  );
};

export default Settings;